[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Disable build cache to ensure latest code is always used
buildCache = false

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
