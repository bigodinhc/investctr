# ===========================================
# InvestCTR Backend Environment Variables
# ===========================================

# Application
# Values: development, staging, production
ENVIRONMENT=development
DEBUG=true

# Database (Supabase PostgreSQL) - REQUIRED
# Get connection string from Supabase Dashboard > Settings > Database
DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# Supabase - REQUIRED
# Get from Supabase Dashboard > Settings > API
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# Redis - REQUIRED for Celery
# Railway: Use the Redis service connection URL
# Local: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# JWT Secret - REQUIRED
# Get from Supabase Dashboard > Settings > API > JWT Secret
JWT_SECRET=your-jwt-secret

# Claude API (Anthropic) - REQUIRED for PDF parsing
# Get from console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxx

# LSEG Market Data (optional - for additional market data)
LSEG_APP_KEY=
LSEG_USERNAME=
LSEG_PASSWORD=

# Celery - REQUIRED for background tasks
# Railway: Use the same Redis URL with different db numbers
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# CORS - REQUIRED
# Development: ["http://localhost:3000"]
# Production: ["https://investctr.vercel.app", "https://your-domain.com"]
CORS_ORIGINS=["http://localhost:3000"]

# ===========================================
# Production Checklist (Railway):
# ===========================================
# 1. Set ENVIRONMENT=production
# 2. Set DEBUG=false
# 3. Configure DATABASE_URL from Supabase (use connection pooler for production)
# 4. Set all Supabase keys (URL, ANON_KEY, SERVICE_KEY, JWT_SECRET)
# 5. Configure Redis URLs from Railway Redis service
# 6. Set ANTHROPIC_API_KEY
# 7. Update CORS_ORIGINS with production frontend URLs
# 8. Ensure health check is configured at /health
